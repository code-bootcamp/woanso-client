import styled from "@emotion/styled";

const BigText = styled.p`
  font-size: 1rem;
`;

const SmallText = styled.span`
  font-size: 1rem;
`;

export const BookTitle = styled(BigText)`
  font-weight: 500;
`;

export const BookInfo = styled.p`
  font-size: 1rem;
`;
